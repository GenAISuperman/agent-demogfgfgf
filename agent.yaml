agent:
  id: "demogfgfgf"
  team: "devai"
  runtime: aks
  interaction_model: "standalone"
  guardrail_profile: "strict"

identity:
  sp_client_id: "2c6c1781-6132-428d-a5c3-46a384eb9c21"
  keyvault_url: "https://kv-shop-poc.vault.azure.net/"

tools: []

evaluation:
  suite: default-strict
  ci_gate: true
  pass_threshold: 0.95
  datasets:
    - id: golden-dataset
      version: v1.0
      source: repo
      path: evals/datasets/golden-dataset.json

output_policy:
  violation_threshold: 5
  window_minutes: 1
  action: alert
  notify_webhook: 

prompts:
  active_version: v1.0
  source: prompts/system.txt
